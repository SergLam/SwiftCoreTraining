{% include 'header' %}

import SnapKit
import UIKit

final class {{ module_info.name }}CollectionViewCellViewModel {
    
    var item: {{ custom_parameters.dto }}
    
    init(_ item: {{ custom_parameters.dto }}) {
        self.item = item
    }
}

final class {{ module_info.name }}CollectionViewCell: UICollectionViewCell {
    
    static let reuseIdentifier: String = String(describing: {{ module_info.name }}CollectionViewCell.self)
    
    // MARK: - Lifecycle
    override init(frame: CGRect) {
        super.init(frame: frame)
        setupLayout()
    }
    
    override func prepareForReuse() {
        super.prepareForReuse()
    }
    
    required init?(coder: NSCoder) {
        fatalError("init(coder:) has not been implemented")
    }
    
    // MARK: - Public functions
    func update(_ model: {{ module_info.name }}CollectionViewCellViewModel) {
        
    }
    
    // MARK: - Private functions
    private func setupLayout() {
        
        backgroundColor = .clear
    }
    
}
